# AI_USAGE.md — 多智能体协作研发工作流说明

> 本文档面向技术评估与面试场景，阐述本仓库所实践的 **AI 增强型多智能体协作研发体系**：如何通过角色职责隔离、变更治理、质量门禁与可追溯机制，把大模型从"单点代码生成器"升级为"受治理的工程生产力"。重点在工作流本身，而非业务功能实现。

---

## 1. 体系定位

本仓库运行着一套 **AI 协作工程协议（`.ai/AGENTS.md`）**，将传统软件工程治理（变更管理、评审、CI、分支策略）与多智能体执行（规划/构建/审查/走查）深度耦合，形成：

- **职责隔离**：每个 AI 角色拥有最小化权限与不可越界的职责边界；
- **变更治理**：按影响面分级（L0~L4），从"直接任务"到"架构变更需 Human 审批"；
- **质量门禁**：PR + 独立 Reviewer + CI（lint/test/build）三道闸门；
- **可追溯**：需求与架构历史由 CR 文档保存，代码历史由 Git 保存，事实层级单一权威。

一句话概括：**把不可控的"AI 生成"收敛为可控的"AI 协作交付"**。

---

## 2. 角色体系与权限矩阵

| 角色 | 定位 | 职责 | 权限边界 |
|---|---|---|---|
| Human | 产品与架构决策者 | 方向、审批、最终合并 | 最终否决权 |
| Planner（规划） | 需求分析/方案/拆解 | CR 文档、任务卡、架构提案 | 仅可写 `.ai/` 文档，不可写代码 |
| Builder（构建） | 任务实现 | 按任务卡实现 + 验证 + PR | 仅任务分支；不可直接 push 主分支 |
| Reviewer（审查） | 独立质量验证 | 按 PR 审查 diff、复跑测试、判定 PASS/REQUEST CHANGES | `edit: deny`——只能评审，不能代写 |
| UI/UX（可选） | 设计走查 | 提案与符合性核验 | 仅设计文档 |

**核心工程理念**：权限最小化 + 审查者与实现者分离（类似 code review 的组织隔离，防止自审自批）；每个 agent 通过独立的提示词规范（`.opencode/agent/*.md`）固化角色行为，而非依赖临时指令。

---

## 3. 变更治理：Change Level 与 CR

- **分级审批**：L0（文案/样式）直接任务；L1（单功能）直接任务；L2（完整能力）→ CR + Requirements + Tasks + Review；L3（架构/数据契约）→ CR + Human 批准；L4（产品方向）→ 产品提案 + Human 决策。
- **CR 文档体系**（`.ai/CHANGES/CR-XXX/`）：CHANGE（Why/Goal/Impact）、REQUIREMENTS（可验证行为 + 验收标准）、ARCHITECTURE/DESIGN（delta，仅记变更不复制基线）、TASKS（可执行单元）、REVIEW（审查产物）。
- **事实层级（Single Source of Truth）**：Human 决定 > `.ai/CURRENT/`（已批准事实）> 已批准 CR > 主线代码 > 分支代码 > 推测。**Agent 不得为迎合实现而修改更高层事实**——禁止"为了跑通而改需求"。
- **需求变更不可静默**：CR 目标改变必须新增 REVISION 文档保留历史，而非改写既有记录。

这形成了**需求-设计-实现-验收的端到端可追溯链**：任一代码行为均可回溯到具体 R-ID 与 CR。

---

## 4. Git 与合并治理（15 条规则）

- **主分支即状态**：main 永远代表可接受状态；Agent 禁止直接 push main。
- **每任务独立分支**：命名含 CR + Task ID（`feature/CR-XXX-T-YYY-slug`），Builder 可多 commit，完成后必须创建 PR。
- **Reviewer 审 PR 而非工作树**：`gh pr checkout` 后独立复跑测试，结论写入 REVIEW 文档。
- **CI 自动门禁**：push/PR 触发 lint + test + build（GitHub Actions），与 Reviewer 人工门禁构成双保险。
- **Human 拥有最终合并权**；合并后删除任务分支（杜绝分支泄漏）。
- **冲突显式解决**：禁止 Agent 静默覆盖他人修改。
- **需求/架构历史归 CR、代码历史归 Git**，两者正交、互不污染。

---

## 5. 任务执行与质量门禁

### 5.1 任务卡（契约式派发）

每个任务卡包含：Objective、Context pack（适用的 R-ID/关键契约/参考模式/禁止项）、Scope/Out of Scope、可验证的 Acceptance criteria、Test requirements、DoD。**Builder 只读任务卡 + 环境事实单点（ENVIRONMENT.md）**，避免重复读取全量上下文——这本质是**上下文工程（context engineering）**：把"该读什么"结构化，压缩 token 成本与理解漂移。

### 5.2 极简派发指令（Prompt 工程实践）

派发 prompt 被收敛为 ≤300 字符的"增量事实"（任务 ID + 分支 + 卡片外信息），其余全部指向权威文档。经过多轮迭代验证：**信息密度与执行质量正相关，与 prompt 长度无必然关系**——长 prompt 带来的冗余反而引入复述不一致风险。

### 5.3 步数预算与检查点（失控防护）

- 每个 agent 配置 **expected steps**（预期步数）与**硬检查点**（如 Builder 40 步）。
- 超过预期步数 → 触发 **TASK HEALTH WARNING**：自我评估 5 问（范围是否膨胀？架构是否不足？是否需要外部调研？是否反复失败？是否应拆分？）后选择 Continue 或 Escalate。
- 达到硬检查点 → 必须写入 `T-XXX-CHECKPOINT.md`（Completed / Current approach / Failed approaches / Remaining / Blocker / Recommendation），**状态可交接给新会话继续**，杜绝"85 步失控会话无限膨胀"。
- 实战验证：多次任务在检查点触发时实现已 100% 完成，收尾交接成本 < 10 分钟。

### 5.4 BLOCKED 协议（不猜测原则）

信息不足或契约不清时，Agent 按固定格式报告 BLOCKED（事实 / 已确认理解 / 选项 / 建议），**绝不猜测绕过**——把不确定性显式上抛给决策层，而不是隐式扩散到代码里。

### 5.5 场景矩阵测试（回归防护）

对于涉及批量导入 / 多文件数据流的任务，强制要求**场景矩阵测试**（R-020）：构造导入组合矩阵（同系列 / 跨系列 / 缺字段 / 混合患者 / 损坏文件），以字节级真实 fixture 走完整产品管线，并断言**解析范围**（mock fetch 调用集合）等行为级不变量——远超"点一下不报错"的冒烟级别。

### 5.6 Reviewer 独立门禁

Reviewer 以 PR diff 为唯一审查对象，独立复跑 `verify.ps1`（test + build），输出 PASS / REQUEST CHANGES + file:line 级发现。**阻塞项必须闭环**：例如某次审查拦截了"远程 AI 请求泄露患者标识符（PHI）"的隐私缺陷，由实现方修复并补单测后才放行合并——证明门禁拦截真实缺陷而非走形式。

---

## 6. 上下文与成本治理

- **环境事实单点**：ENVIRONMENT.md 固化平台/工具链/验证命令/分支纪律，消除各会话重复探索（实测将派发指令从 3.7k 字符降到 <300）。
- **统一验证脚本**：`scripts/verify.ps1` 一键 PATH 刷新 + 全量测试 + 构建，避免 agent 逐条解释环境问题。
- **模型路由（成本治理）**：按任务不确定性分档——简单 UI/测试/文档任务走低成本模型（flash），核心解析/架构任务走高能力模型；审查任务单独路由以保证独立视角。实测将成本热点（Builder 层）显著下移。
- **Spike 门禁**：高不确定性任务（新库/新解析器）先做受限研究任务（Spike）并向 Human 汇报方向，获批后才展开实现——把"探索型试错"从高价模型会话中剥离。
- **测试分层策略**：核心层（领域/解析/契约）全量测试保留；UI 组件按"每组件 3~5 个关键交互"收敛；视觉重构类任务只要求存量不回归 + 冒烟。既守住回归防线，又控制维护成本。

---

## 7. 安全与合规边界

- **隐私边界（PHI 过滤）**：AI 远程调用在请求构造层做**白名单过滤**（`toRemoteDicomMeta`），患者姓名/ID 等直接标识符绝不外发；该边界有单测断言与文档声明双重保障。
- **密钥治理**：API Key 仅存本机浏览器 localStorage（独立 key），**绝不进入导出 JSON / 日志 / 代码仓库**；文档明确泄露处置流程。
- **失败回退策略**：真实 API 失败/超时自动回退确定性 Mock，且**界面如实明示来源**（真实 API / Mock 生成），不"悄悄换源"。
- **产品边界**：医疗相关功能仅做工程素材整理与验证，禁止生成任何诊断/治疗结论——AI 输出均带"Mock/模拟"明示与拒绝路径。

---

## 8. 可追溯与度量

- **双轨历史**：CR 文档（为什么/需求/决策）与 Git 历史（代码/何时）正交；`CURRENT/` 为已批准事实的唯一入口。
- **技术债显式化**：非阻塞问题登记 `.ai/TODO.md`（含建议方向与来源），杜绝"顺手修"导致的 diff 污染。
- **过程度量（截至 v0.8.1）**：
  - 15 个 CR 里程碑、60+ 个 PR 全部经 PR + Reviewer + CI 三道门禁合并；
  - 自动化测试 456 例（44 文件），其中场景矩阵覆盖批量导入等高频故障域；
  - 多次执行失控防护（步数检查点、BLOCKED 协议、隐私门禁拦截）有真实拦截记录可复盘。

---

## 9. 结论

这套体系的核心结论是：**AI 能力的工程化价值不在"生成代码"，而在"被治理地生成代码"**。通过角色权限分离、变更分级、双重门禁（CI + 独立审查）、失控防护与成本路由，我们把大模型的强生产能力约束在可审查、可回滚、可追溯的工程边界内——这也是本仓库在规模化 AI 协作研发方向上的实践样本。

---

*关联文件：`.ai/AGENTS.md`（协作协议）、`.ai/AGENTS/`（角色规范）、`.ai/CHANGES/`（CR 历史）、`.opencode/agent/`（角色配置）、`ENVIRONMENT.md`（环境事实）。*